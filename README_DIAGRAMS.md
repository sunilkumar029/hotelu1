# 🔄 Visual Architecture & Flow Diagrams

## Application Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                      FRONTEND (React)                        │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ • Login Component                                    │   │
│  │ • User Management Page                              │   │
│  │ • Dashboard, Orders, Inventory, etc.               │   │
│  │                                                      │   │
│  │ localStorage: { authToken, currentUser }            │   │
│  └──────────────────────────────────────────────────────┘   │
│                           ↓ HTTP                             │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │          BACKEND SERVER (Node.js/Express)          │    │
│  │  ┌──────────────────────────────────────────────┐   │    │
│  │  │ Routes & Middleware                          │   │    │
│  │  │ • POST /login → JWT Token                    │   │    │
│  │  │ • verifyToken Middleware                     │   │    │
│  │  │ • Protected Endpoints (require token)        │   │    │
│  │  └──────────────────────────────────────────────┘   │    │
│  │                     ↓                                │    │
│  │  ┌──────────────────────────────────────────────┐   │    │
│  │  │ API Endpoints                                │   │    │
│  │  │ • /api/users (admin only)                    │   │    │
│  │  │ • /api/orders (authenticated)                │   │    │
│  │  │ • /api/menu (public)                         │   │    │
│  │  │ • /api/inventory (authenticated)             │   │    │
│  │  └──────────────────────────────────────────────┘   │    │
│  │                     ↓                                │    │
│  │  ┌──────────────────────────────────────────────┐   │    │
│  │  │ Database Connection (Sequelize ORM)          │   │    │
│  │  └──────────────────────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────┘    │
│                           ↓                                  │
├─────────────────────────────────────────────────────────────┤
│                    MYSQL DATABASE                            │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ • Users Table (username, password, role, name)     │    │
│  │ • Orders Table (id, status, total, items, etc.)    │    │
│  │ • OrderItems Table (links to Orders & MenuItems)   │    │
│  │ • MenuItems Table (name, price, category, desc.)   │    │
│  │ • Inventories Table (name, stock, minStock)        │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## Authentication Flow

```
┌──────────────────┐
│  User Enters     │
│  Credentials     │
└────────┬─────────┘
         │ username: "admin"
         │ password: "admin"
         ↓
   ┌─────────────────────┐
   │ POST /login         │
   │ {username, password}│
   └────────┬────────────┘
            │
            ↓
   ┌──────────────────────────────┐
   │ Backend: Validate Credentials│
   │ Check defaultUsers object    │
   └────────┬─────────────────────┘
            │
            ├─ Valid?
            │
            ↓ YES
   ┌──────────────────────────┐
   │ JWT.sign(user, SECRET,   │
   │ {expiresIn: "24h"})      │
   │ Returns: {               │
   │   token: "eyJhbGc...",   │
   │   user: {...}            │
   │ }                        │
   └────────┬─────────────────┘
            │
            ↓
   ┌──────────────────────────────┐
   │ Frontend: Store Token        │
   │ localStorage.setItem(        │
   │   'authToken', token         │
   │ )                            │
   └────────┬─────────────────────┘
            │
            ↓
   ┌──────────────────────────────────┐
   │ Frontend: All Future Requests    │
   │ Authorization: Bearer {token}    │
   └────────┬─────────────────────────┘
            │
            ↓
   ┌──────────────────────────────────────────┐
   │ Backend: Receive Request with Token      │
   │ verifyToken Middleware:                  │
   │  1. Extract token from header            │
   │  2. JWT.verify(token, SECRET)            │
   │  3. Decode and attach to req.user        │
   │  4. Call next() if valid                 │
   └────────┬─────────────────────────────────┘
            │
            ├─ Valid?
            │
            ├─ YES → Process Request
            │
            └─ NO → 401 Unauthorized
```

---

## Protected Endpoint Access Flow

```
Request Arrives: POST /api/users

                    ↓

    ┌───────────────────────────────┐
    │ Step 1: Extract JWT Token     │
    │ from Authorization Header     │
    │ "Bearer eyJhbGc..."           │
    └────────────┬──────────────────┘
                 │
                 ↓
    ┌───────────────────────────────┐
    │ Step 2: Verify Token Signature│
    │ JWT.verify(token, SECRET)     │
    │ (check if not expired)        │
    └────────────┬──────────────────┘
                 │
         ┌───────┴────────┐
         │                │
      VALID?           INVALID
         │                │
         ↓                ↓
    ✅ Decoded      ❌ 401 Error
    req.user = {...}   End Request
         │
         ↓
    ┌───────────────────────────────┐
    │ Step 3: Check Authorization   │
    │ Is req.user.role === 'admin'? │
    └────────────┬──────────────────┘
                 │
         ┌───────┴────────┐
         │                │
       ADMIN?          NOT ADMIN
         │                │
         ↓                ↓
    ✅ Continue    ❌ 403 Forbidden
    to route          End Request
         │
         ↓
    ┌───────────────────────────────┐
    │ Step 4: Process Request       │
    │ (Database operations)         │
    │ Create/Update/Delete users    │
    └────────────┬──────────────────┘
                 │
                 ↓
    ┌───────────────────────────────┐
    │ Step 5: Return Response       │
    │ Status 201: User Created      │
    │ {user: {...}}                 │
    └───────────────────────────────┘
```

---

## Complete Request-Response Cycle

```
┌─────────────────────┐
│     FRONTEND        │
│   (React App)       │
└──────────┬──────────┘
           │
    ┌──────┴──────┐
    │ Login Form  │
    │ Submit      │
    └──────┬──────┘
           │
           ├─ username: admin
           ├─ password: admin
           │
           ↓
    ┌────────────────────────────┐
    │ POST http://localhost:3001/│
    │         login              │
    │ Content-Type: application/ │
    │           json             │
    │                            │
    │ Body:                      │
    │ {username, password}       │
    └──────────┬─────────────────┘
               │
               ↓
    ┌─────────────────────────────────────┐
    │        BACKEND SERVER               │
    │                                     │
    │ 1. Receive request                  │
    │ 2. Parse body (username, password)  │
    │ 3. Find user in defaultUsers        │
    │ 4. Compare password                 │
    │ 5. If match:                        │
    │    - Generate JWT token             │
    │    - Sign with SECRET               │
    │    - Set 24h expiration             │
    │ 6. Return response                  │
    └──────────┬──────────────────────────┘
               │
               ├─ Status: 200 OK
               │
               ├─ Response Body:
               │  {
               │    message: "Login successful",
               │    token: "eyJhbGciOiJIUzI1NiI...",
               │    user: {
               │      username: "admin",
               │      role: "admin",
               │      name: "Admin User"
               │    }
               │  }
               │
               ↓
    ┌─────────────────────────────┐
    │      FRONTEND               │
    │                             │
    │ 1. Receive response         │
    │ 2. Extract token            │
    │ 3. Store in localStorage:   │
    │    authToken = "eyJhbGc..." │
    │ 4. Set user state           │
    │ 5. Navigate to Dashboard    │
    │ 6. Show success message     │
    └─────────────────────────────┘


SUBSEQUENT REQUESTS (with Token):

┌─────────────────────────────────────┐
│ GET http://localhost:3001/api/users │
│                                     │
│ Headers:                            │
│  Authorization: Bearer eyJhbGc...   │
│  Content-Type: application/json     │
└──────────┬──────────────────────────┘
           │
           ↓
┌──────────────────────────────────────┐
│     BACKEND SERVER                   │
│                                      │
│ 1. Receive request                   │
│ 2. Middleware: verifyToken           │
│    - Extract token from header       │
│    - JWT.verify(token, SECRET)       │
│    - Attach decoded data to req.user │
│ 3. Check req.user.role === 'admin'   │
│ 4. If authorized:                    │
│    - Query database for users        │
│    - Return user list                │
│ 5. If unauthorized:                  │
│    - Return 403 Forbidden            │
└──────────┬───────────────────────────┘
           │
           ├─ Status: 200 OK
           │
           ├─ Response Body:
           │  [
           │    {username, role, name},
           │    {username, role, name},
           │    ...
           │  ]
           │
           ↓
┌──────────────────────────────────────┐
│      FRONTEND                        │
│                                      │
│ 1. Receive response                  │
│ 2. Parse user list                   │
│ 3. Display in table                  │
│ 4. Show success message              │
└──────────────────────────────────────┘
```

---

## Database Operation Flow

```
API Request with Data
        ↓
   ┌────────────────┐
   │ Validate Input │
   │ Check required │
   │ fields         │
   └────────┬───────┘
            │
      ┌─────┴─────┐
      │            │
    Valid?      Invalid
      │            │
      ↓            ↓
  Continue    Return Error
      │        (400 Bad Request)
      │
      ↓
   ┌─────────────────────────┐
   │ Check Authorization     │
   │ Verify user role        │
   │ (admin for certain ops) │
   └────────┬────────────────┘
            │
      ┌─────┴──────┐
      │             │
   Allowed?     Denied
      │             │
      ↓             ↓
  Continue    Return Error
      │        (403 Forbidden)
      │
      ↓
   ┌─────────────────────────────┐
   │ Database Operation          │
   │                             │
   │ CREATE / UPDATE / DELETE /  │
   │ SELECT (via Sequelize ORM)  │
   └────────┬────────────────────┘
            │
      ┌─────┴─────────┐
      │               │
   Success?        Error?
      │               │
      ↓               ↓
   ✅ OK        Return Error
   Process      (500 Server Error)
   Result
      │
      ↓
   ┌──────────────────────┐
   │ Format Response      │
   │ Return JSON          │
   │ Set Status Code      │
   │ (201, 200, etc.)     │
   └──────────────────────┘
```

---

## File Organization

```
kiran/
│
├── 📚 DOCUMENTATION (Read These)
│   ├── QUICK_REFERENCE_CARD.md ⭐ START HERE
│   ├── QUICK_START_GUIDE.md
│   ├── API_TESTING_GUIDE.md
│   ├── AUDIT_SUMMARY.md
│   ├── ISSUES_AND_SOLUTIONS.md
│   ├── COMPLETE_AUDIT_REPORT.md
│   └── README-DIAGRAMS.md (this file)
│
├── 🧪 TESTING
│   ├── api-test-complete.js (automated tests)
│   └── test-api.js (old, without JWT)
│
├── 📂 BACKEND
│   ├── server.js (current)
│   ├── server-improved.js (better error handling)
│   ├── package.json
│   ├── models/
│   │   ├── sequelize.js (DB config)
│   │   ├── User.js
│   │   ├── Order.js
│   │   ├── MenuItem.js
│   │   ├── OrderItem.js
│   │   └── Inventory.js
│   └── scripts/
│       └── seedUsers.js
│
├── 📂 FRONTEND
│   ├── package.json
│   ├── src/
│   │   ├── components/
│   │   │   ├── App.jsx
│   │   │   ├── Login.jsx ← Gets token here
│   │   │   ├── UserManagement.jsx
│   │   │   ├── Dashboard.jsx
│   │   │   └── ...other components
│   │   └── utils/
│   │       └── api.js ← Sends token in headers
│   └── public/
│       └── index.html
│
└── 🔧 CONFIG
    └── (none currently, should add .env)
```

---

## Testing Methods Comparison

```
┌─────────────────────────────────────────────────────────────────┐
│                    AUTOMATED TESTS                               │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Command: node api-test-complete.js                      │   │
│  │ Speed: ⚡⚡⚡ (5 seconds)                                │   │
│  │ Coverage: ⭐⭐⭐ (All endpoints)                         │   │
│  │ Pros:                                                    │   │
│  │  • Tests everything in one command                       │   │
│  │  • Includes JWT token generation                         │   │
│  │  • Shows all responses                                   │   │
│  │  • Good for verification                                 │   │
│  │ Cons:                                                    │   │
│  │  • Can't test single endpoint                            │   │
│  │  • Can't modify requests easily                          │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    CURL COMMANDS                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Command: curl -X GET http://localhost:3001/api/users \  │   │
│  │          -H "Authorization: Bearer {token}"             │   │
│  │ Speed: ⚡⚡ (requires manual token copy)               │   │
│  │ Coverage: ⭐⭐⭐ (Any endpoint)                         │   │
│  │ Pros:                                                    │   │
│  │  • Test individual endpoints                             │   │
│  │  • Full control over request                             │   │
│  │  • Quick for verification                                │   │
│  │ Cons:                                                    │   │
│  │  • Manual token management                               │   │
│  │  • Longer commands                                       │   │
│  │  • Harder to test complex scenarios                      │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    POSTMAN UI                                    │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Method: Visual interface                                │   │
│  │ Speed: ⚡ (UI overhead)                                 │   │
│  │ Coverage: ⭐⭐⭐ (Any endpoint)                         │   │
│  │ Pros:                                                    │   │
│  │  • User-friendly interface                               │   │
│  │  • Auto-format JSON responses                            │   │
│  │  • Save requests as collection                           │   │
│  │  • Test scenarios with flow                              │   │
│  │ Cons:                                                    │   │
│  │  • Slower than CLI                                       │   │
│  │  • Requires Postman installation                         │   │
│  │  • Learning curve                                        │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND UI                                   │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │ Method: React app in browser                            │   │
│  │ Speed: ⚡ (JavaScript framework)                        │   │
│  │ Coverage: ⭐⭐ (Only available pages)                   │   │
│  │ Pros:                                                    │   │
│  │  • Most realistic testing                                │   │
│  │  • User experience matches real usage                    │   │
│  │  • Integrated experience                                 │   │
│  │ Cons:                                                    │   │
│  │  • Limited to what's built in UI                         │   │
│  │  • Can't test all endpoints                              │   │
│  │  • Browser/JavaScript overhead                           │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Status Indicators Guide

```
┌────────────────────────────────────────────────────────┐
│              SERVER STARTUP INDICATORS                 │
└────────────────────────────────────────────────────────┘

✅ SUCCESS (Server Ready):
   ✅ MySQL connection established.
   ✅ Database models synchronized successfully.
   ✅ Backend server running at http://localhost:3001

⚠️ WARNING (Check Issues):
   ⚠️ Unable to connect to MySQL
   ⚠️ Check server logs for connection details

❌ ERROR (Server Won't Start):
   ❌ Unable to connect to MySQL: ECONNREFUSED
   ❌ Make sure database 'mrbeast_db' exists
   ❌ Verify credentials in backend/models/sequelize.js

✅ API RESPONSE INDICATORS:
   ✅ 200 OK - Request successful
   ✅ 201 Created - Resource created
   ✅ 400 Bad Request - Invalid input
   ✅ 401 Unauthorized - Missing/invalid token
   ✅ 403 Forbidden - Permission denied
   ✅ 404 Not Found - Resource/endpoint not found
   ✅ 500 Server Error - Backend error
   ✅ 503 Service Unavailable - Database not connected
```

---

## Quick Decision Tree

```
┌─ Do I want to test the API? ─┐
│                             │
├─ YES ─┐                     │
│       └─ Do I have a token? ─┐
│                             │
│         ├─ NO ─┐            │
│         │      └─ Login first: POST /login
│         │                  │
│         └─ YES ─┐          │
│                 └─ Choose testing method:
│                            │
│         ┌─────────┬────────┴──────────┬─────────┐
│         │         │                   │         │
│      Speed?    Ease?         Need UI?    Need Full Test?
│         │         │                   │         │
│      Quick    Easy              Yes        Yes
│         │         │                   │         │
│         ↓         ↓                   ↓         ↓
│      cURL     Postman          Frontend   api-test-complete.js
│         │         │                   │         │
│         └─────────┴───────────────────┴─────────┘
│                   │
│              Run TEST
│                   │
│                   ↓
│      Check if you got SUCCESS response
│
└─ NO ─┐
       └─ Use the FRONTEND to test
            (login → navigate → use features)
```

---

This completes the comprehensive audit with visual diagrams! 📊
